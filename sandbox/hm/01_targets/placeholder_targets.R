
tar_target(name = ind_sst,
           command = {
             ind_placeholder(ind_name="Sea Surface Temperature", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_temp_at_depth,
           command = {
             ind_placeholder(ind_name="Temperature at Depth", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_sea_surface_salinity,
           command = {
             ind_placeholder(ind_name="Sea Surface Salinity", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_subsurface_salinity,
           command = {
             ind_placeholder(ind_name="Subsurface Salinity", areas = MPAs)
           }), # Environmental Representativity

tar_target(name = ind_oxygen_saturation,
           command = {
             ind_placeholder(ind_name="Oxygen Saturation", areas = MPAs)
           }), # Environmental Representativity

tar_target(name = ind_ave_ph_level,
           command = {
             ind_placeholder(ind_name="Average pH Level", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_nitrate,
           command = {
             ind_placeholder(ind_name="Nitrate", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_carbonate,
           command = {
             ind_placeholder(ind_name="Carbonate", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_ave_mixed_layer_depth,
           command = {
             ind_placeholder(ind_name="Average Mixed Layer Depth", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_ave_position_of_shelf_slope_front,
           command = {
             ind_placeholder(ind_name="Average Position of Shelf Slope Front", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_wind_speed_and_storminess,
           command = {
             ind_placeholder(ind_name="Wind Speed and Storminess", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_chlorophyll_a,
           command = {
             ind_placeholder(ind_name="Chlorophyll a", areas = MPAs)
           }), # Structure and Function, maybe Biomass Metrics


tar_target(name = ind_phytoplankton_biomass_and_diversity,
           command = {
             ind_placeholder(ind_name="Phytoplankton Biomass and Diversity", areas = MPAs)
           }), # Biomass Metrics, maybe species/functional Diversity


tar_target(name = ind_start_date_spring_bloom,
           command = {
             ind_placeholder(ind_name="Start Date of Spring Bloom", areas = MPAs)
           }), # Structure and Function??


tar_target(name = ind_biomass_calanus_finmarchicus,
           command = {
             ind_placeholder(ind_name="Biomass of Calanus finmarchicus  ", areas = MPAs)
           }), # Biomass Metrics


tar_target(name = ind_zooplankton_community_composition,
           command = {
             ind_placeholder(ind_name="Zooplankton Community Composition", areas = MPAs)
           }), # Functional Diversity??


tar_target(name = ind_fish_eggs_and_larve,
           command = {
             ind_placeholder(ind_name="Fish Eggs and Larve", areas = MPAs)
           }), # Biomass Metrics?


tar_target(name = ind_seasonal_presence_cetaceans,
           command = {
             ind_placeholder(ind_name="Seasonal Presence/Absence of Cetaceans", areas = MPAs)
           }), # Connectivity

tar_target(name = ind_seasonal_presence_seals,
           command = {
             ind_placeholder(ind_name="Seasonal Presence/Absence of Seals", areas = MPAs)
           }), # Connectivity


tar_target(name = ind_seasonal_presence_pelagics,
           command = {
             ind_placeholder(ind_name="Seasonal Presence/Absence of Large Pelagics", areas = MPAs)
           }), # Connectivity


tar_target(name = ind_seasonal_presence_seabirds,
           command = {
             ind_placeholder(ind_name="Seasonal Presence/Absence of Seabirds", areas = MPAs)
           }), # Connectivity


tar_target(name = ind_proportion_demersal_fish,
           command = {
             ind_placeholder(ind_name="Proportion of Large and Small Demersal Fish Species", areas = MPAs)
           }), # Structure and Function


tar_target(name = ind_abundance_invasive_species_webmr,
           command = {
             ind_placeholder(ind_name="Abundance of Aquatic Invasive Species in WEBMER", areas = MPAs)
           }), # Structure and Function


tar_target(name = ind_biomass_groundfish_prey_webmr,
           command = {
             ind_placeholder(ind_name="Biomass of Groundfish Prey Ppecies within WEBMR", areas = MPAs)
           }), # Biomass Metrics, Structure and Function


tar_target(name = ind_abundance_sea_pens_webmr,
           command = {
             ind_placeholder(ind_name="Abundance of Sea Pens within WEBMR", areas = MPAs)
           }), # Functional Diversity


tar_target(name = ind_richness,
           command = {
             ind_placeholder(ind_name="Diversity of Epibenthic and Infaunal Communities", areas = MPAs)
           }), # Species Diversity


tar_target(name = ind_community_comp_epibenthic_infaunal,
           command = {
             ind_placeholder(ind_name="Community Composition of Epibenthic and Infaunal Benthic Communities", areas = MPAs)
           }), # Structure and Function?


tar_target(name = ind_biomass_bioturbators,
           command = {
             ind_placeholder(ind_name="Biomass of Bioturbators", areas = MPAs)
           }), # Structure and Function?, Biomass Metrics?

tar_target(name = ind_spatial_extent_ebsa_webmr,
           command = {
             ind_placeholder(ind_name="Spatial Extent of EBSA with WEBMR", areas = MPAs)
           }), # Environmental Representativity


tar_target(name = ind_rel_abundance_groundfish,
           command = {
             ind_placeholder(ind_name="Relative abundance and biomass of select groundfish species", areas = MPAs)
           }), # Biomass Metrics


tar_target(name = ind_size_distribution_groundfish,
           command = {
             ind_placeholder(ind_name="Size Distribution of Select Groundfish Species", areas = MPAs)
           }), # Biomass Metrics??


tar_target(name = ind_condition_groundfish,
           command = {
             ind_placeholder(ind_name="Condition of Select Groundfish Species", areas = MPAs)
           }), # Biomass Metrics??


tar_target(name = ind_fecundity_groundfish,
           command = {
             ind_placeholder(ind_name="Fecundity of Select Groundfish Species", areas = MPAs)
           }), # Biomass Metrics


tar_target(name = ind_community_comp_demersal,
           command = {
             ind_placeholder(ind_name="Community Composition of Demersal Fish", areas = MPAs)
           }), # Structure and Function


tar_target(name = ind_distribution_juv_haddock_habitat,
           command = {
             ind_placeholder(ind_name="Distriburion of Juvenile Haddock Habitat", areas = MPAs)
           }), # Key Fish Habitat


tar_target(name = ind_distribution_key_fish_habitat,
           command = {
             ind_placeholder(ind_name="Distribution of Key Fish Species Habitat", areas = MPAs)
           }), # Key Fish Habitat


tar_target(name = ind_unauthorized_fishing,
           command = {
             ind_placeholder(ind_name="Amount of Unauthorized Fishing", areas = MPAs)
           }), # Threats to Productivity


tar_target(name = ind_seabed_disruption_in_around_webmr_fishing,
           command = {
             ind_placeholder(ind_name="Seabed area in and surrounding WEBMR swept by bottom-tending commercial fishing gear", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_total_annual_landings,
           command = {
             ind_placeholder(ind_name="Total annual landings form 4W for directed groundfish fisheries and common bycatch only stocks", areas = MPAs)
           }), # Threats to Productivity


tar_target(name = ind_seabed_disruption_in_around_webmr_research,
           command = {
             ind_placeholder(ind_name="Seabed area in and surrounding WEBMR swept by bottom-tending commercial fishing gear for research and monitoring", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_vessel_traffic,
           command = {
             ind_placeholder(ind_name="Vessel Traffic Intensity", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_ocean_sound,
           command = {
             ind_placeholder(ind_name="Ocean Sound", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_cables,
           command = {
             ind_placeholder(ind_name="Number of cables by type", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_well_proximity,
           command = {
             ind_placeholder(ind_name="Number of wells in proximity to WEBMR", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_offshore_wind_developments,
           command = {
             ind_placeholder(ind_name="Number of offshore wind developments within in the vacinity of WEBMR", areas = MPAs)
           }), # Threats to Habitat, Threats to Productivity


tar_target(name = ind_contaminant_concentration,
           command = {
             ind_placeholder(ind_name="Concentrations of Contaminants by Type", areas = MPAs)
           }), # Threats to Productivity, Threats to Habitat


tar_target(name = ind_anthropogenic_debris,
           command = {
             ind_placeholder(ind_name="Quantity of Anthropogenic Debris", areas = MPAs)
           }), # Threats to Habitat


tar_target(name = ind_cumulative_impact,
           command = {
             ind_placeholder(ind_name="Change in Relative Cumulative Impact", areas = MPAs)
           }), # Threats to Productivity, Threats to Habitat














